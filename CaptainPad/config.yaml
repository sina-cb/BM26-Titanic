# CaptainPad Configuration
# Default engine address. This is the single source of truth — the app fails
# to boot if this key is missing. Use the Config tab in CaptainPad (writes to
# AsyncStorage) to override per-device at runtime without touching this file.
api_base: "http://127.0.0.1:6968"
