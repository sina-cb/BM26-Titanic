# 371 — BM26 wrap: the event is over, the repo is closed out

Coordinator closure report, operator-ordered ("let's finish and wrap up
BM26"). This is the record that Burning Man 2026 concluded and what state
everything was left in.

## Repo state at wrap

- Local `main` reset to `origin/main` = `439a3201` "Road to Playa - Final
  Final (#56)"; working tree clean. Every coordinated wave from late August
  (party/LIVE bulletproofing `_356`-`_358`, calendar working-day frames
  `_359`, colour-anomaly root causes `_361`, shutdown-blackout safety,
  smokestack switch removal + narrowed forced push `_362`/`_363`-`_368`,
  gamma retirement, HIL tooling) landed in main via PRs #55/#56 before the
  event.
- The operator's final on-playa mapping experiments (one local commit
  "titanic mapping" + 16 working-tree files: titanic/studiodj models,
  controllers/patches/calibration, a deleted preshow snapshot) were
  preserved locally in a stash and the local-only branch
  `backup/bm26_final_titanic_mapping`, then the tree was reset. Delete both
  once confirmed unneeded; nothing was pushed.

## Open threads deliberately left behind

1. `origin/feat/titanic_normalized_2d_views` — operator ruling: SHOULD be
   merged to main; deferred as the one open code task.
2. The show servers (interior + exterior) were unreachable at wrap; their
   scratch workspaces may hold unfetched on-playa git work. When either is
   powered again: `python deploy/deploy.py fetch --machine <name>` collects
   it non-destructively (prod trees carry no git by design).
3. Six content patterns in the private LED-controller repo fail its deploy
   pattern check (`titanic_aurora`/`caustic`/`horizon`/`lighthouse`/
   `shimmer`/`sparkle_rain`, parser "Error at '.'") — every non-skip deploy
   aborts until they are fixed or retired there.

## Runtime

Nothing is running: launcher stack down, coordinator watchdog retired, no
agents in flight. The engine/pad/sim code in main is the code that ran the
show.

Fair winds, Titanic. 🚢
