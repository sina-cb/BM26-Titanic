# Auto-written by MarsinEngine — per-scene audio state.
# Contains:
#   - mic selection  (capture.platform/inputFormat/device/deviceLabel/deviceId/selectedAt)
#   - listener tuning (enabled, fftSize, hopSize, bands{...}, kick{...})
# Written by `engine.js --choose_mic --model <scene>` (mic) and by
# PATCH /audio/config from CaptainPad (tuning). Do not hand-edit while
# the engine is running.
capture:
  device: audio=Microphone Array (Realtek(R) Audio)
  deviceLabel: Microphone Array (Realtek(R) Audio)
  deviceId: dshow-audio-Microphone Array (Realtek(R) Audio)
  inputFormat: dshow
  platform: win32
enabled: true
fftSize: 1024
hopSize: 512
bands:
  lowMaxHz: 200
  midMaxHz: 4000
  attackMs: 8
  releaseMs: 180
  noiseGate: 0.04
  inputGain: 1
kick:
  minHz: 50
  maxHz: 110
  threshold: 1.8
  refractoryMs: 140
  decayMs: 70
structureDetector:
  enabled: true
