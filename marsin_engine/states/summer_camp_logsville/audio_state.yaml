# Auto-written by MarsinEngine — per-scene audio state.
# Migrated 2026-06-14: per-scene analyzer tuning (chains/bands/kick/fft) REMOVED
# so every scene inherits the tuned engine defaults (config.yaml + DEFAULT_CHAINS).
# Only the machine-specific mic capture + the enabled flag are kept per scene.
enabled: true
capture:
  device: audio=Microphone Array (2- Realtek(R) Audio)
  deviceLabel: Microphone Array (2- Realtek(R) Audio)
  deviceId: dshow-audio-Microphone Array (2- Realtek(R) Audio)
  inputFormat: dshow
  platform: win32
